{
	"name": "beeterty/clickhouse-php-client",
	"description": "A lightweight, zero-dependency ClickHouse HTTP client for PHP. Supports fluent query building, JSONEachRow streaming inserts, parameterized queries, and a clean expressive API for interacting with ClickHouse over its native HTTP interface.",
	"type": "library",
	"license": "MIT",
	"keywords": [
		"clickhouse",
		"database",
		"http-client",
		"query-builder",
		"php"
	],
	"authors": [],
	"require": {
		"php": "^8.2",
		"ext-curl": "*",
		"ext-json": "*"
	},
	"require-dev": {
		"friendsofphp/php-cs-fixer": "^3.0",
		"phpbench/phpbench": "^1.6",
		"phpstan/phpstan": "^2.0",
		"phpunit/phpunit": "^11.0"
	},
	"autoload": {
		"psr-4": {
			"Beeterty\\ClickHouse\\": "src/"
		}
	},
	"autoload-dev": {
		"psr-4": {
			"Beeterty\\ClickHouse\\Tests\\": "tests/"
		}
	},
	"config": {
		"sort-packages": true,
		"optimize-autoloader": true
	},
	"minimum-stability": "stable",
	"prefer-stable": true
}
